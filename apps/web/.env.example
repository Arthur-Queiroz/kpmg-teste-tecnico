# Backend API base URL
VITE_API_URL=http://localhost:3000

# Usa a API mock em memória (apps/web/src/api/mockCompanyApi.ts) no lugar do
# backend real. Só para desenvolvimento/demonstração — nunca em produção.
VITE_API_MOCK=false
